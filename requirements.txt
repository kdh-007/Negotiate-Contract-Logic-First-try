requests>=2.31
pypdf>=4.0
olefile>=0.47
# 스캔 이미지 PDF의 OCR 보충용(nego/attachments.py). 시스템에 tesseract
# 바이너리(+ tesseract-ocr-kor 언어팩)가 없으면 자동으로 건너뛴다 — 필수는
# 아니지만 pip 설치 자체는 시스템 바이너리 없이도 된다.
pymupdf>=1.24
pytesseract>=0.3.10
pillow>=10.0
