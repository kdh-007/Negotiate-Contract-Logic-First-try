# 프로젝트 메모

## 2026-09-21 — 유사도 판정 파이프라인 배선 + AI 보조 판단 구현

로드맵 4~5단계(`nego/similarity.py`)를 실제로 연결하고, 로드맵 5단계(AI 보조 판단)도
구현했다. 아직 실제 자동 파이프라인(매일 실행되는 워크플로)에 AI 판단을 상시 켜두지는
않았다 — 비용/속도 때문에 `--ai-similarity` CLI 플래그로 opt-in.

1. **배선**: `config/past_projects.json` → `AppConfig.past_projects` → `pipeline.build_candidates`가
   후보마다 `similarity.score()`를 계산해 `Candidate.similarity`에 저장 → `report.py`가
   표에 "유사도" 칸(원 색상 + 매칭 사업/축별 점수 팝업)으로 표시. 코드 수정 없이
   `config/past_projects.json`만 채우면 다음 실행부터 반영된다.
2. **첫 실제 실적 등록**: 국립생물자원관 어린이체험실 전시 설계 및 제작·설치
   (제안요청서+과업지시서 HWPX 원본에서 `nego/attachments.py`로 텍스트 추출).
3. **AI 보조 판단(`nego/ai_similarity.py`, 신규)**: 내용 유사도(text) 축만 Claude에게
   재판단시켜 동의어/표현 차이를 보정한다. 업역/규모 축은 그대로 코드 판정 유지
   (AI가 더 잘 볼 이유가 없는 영역).
   - 모델 기본값 `claude-haiku-4-5` (환경변수 `SIMILARITY_AI_MODEL`로 교체 가능)
   - `tool_choice`로 `report_similarity` 도구 호출을 강제해 `{score, reason}` JSON을
     안정적으로 받는다.
   - **비용 통제**: 매 파이프라인 실행마다 자동으로 돌지 않는다. `python -m nego
     --ai-similarity`로 opt-in하면, 이미 스크리닝·자격게이트를 통과한 **최종 후보만**
     (공고당 API 호출 1회, 코드가 고른 최유사 과거실적 1건과만 비교) 재판단한다.
     `ANTHROPIC_API_KEY` 필요, 패키지 없거나 키 없으면 에러 메시지 출력 후 종료.
   - 실패(네트워크/rate limit/응답 파싱 오류)해도 파이프라인을 죽이지 않고 기존 자카드
     점수를 그대로 둔다(fail-open, 면허제한정보 API와 같은 패턴).
   - 결과는 `SimilarityResult.text_source="ai"`로 표시되어 코드 판정(`"jaccard"`)과
     구분되고, 리포트 팝업에 🤖 표시 + AI 판단 근거 문장이 따로 붙는다.
   - 테스트 7건 추가(가짜 client로 네트워크 호출 없이 검증).
4. **아직 안 한 것**: 자동 워크플로(GitHub Actions)에 `--ai-similarity`를 상시 켤지는
   결정 안 함 — 과거실적이 몇 건 안 되는 지금은 API 호출량이 적어 상시 켜도 무방해
   보이지만, 비용 대비 효과를 실제로 본 뒤 결정하는 게 나을 듯. `DEFAULT_WEIGHTS`
   (1/3씩 동일가중치)도 여전히 임시값.

## 2026-09-16 수정분 실측 검증 — 부분 확인됨 (2026-09-18, 5일치 수집으로)

30일치 정식 검증은 아직 안 했고, 9/18에 5일치(9/13~9/18) 수집으로 일부만 확인됨.

1. **단양군 미디어아트 공고(R26BK01731335) "자격 미달(조명용제어장치(3912110702))"** — ✅ 확인됨.
   9/18 Run #64에서 정확히 이 문구로 표시됨.
2. **오탐 확인** — 5일치 표본이 작아(참가자격 절 발견 7건 중 판정 반영 3건) 아직 전수 대조 못 함.
   30일치로 다시 돌릴 때 재확인할 것.
3. **경상남도관광재단 K-거상 공고(R26BK01707504) "마감/일정"** — ✅ 확인됨.
   9/18 Run #64에서 "D-10 (첨부파일 제출기한)"으로 정확히 채워짐.

## 결정된 사항 (아직 구현 전, 요청 시 진행)

- **메신저 알림 플랫폼: 텔레그램(Telegram) 봇**으로 결정. 카카오톡은 비즈니스 채널+알림톡 등록·템플릿 심사·건당 비용이 필요해 보류하고, 완전 무료에 승인 절차 없이 바로 되는 텔레그램 봇 API로 진행하기로 함.
  - 목적: 매주 수집 결과를 "수집 완료" 알림이 아니라 **보고서 형태**(후보 목록·발주기관·금액·마감일 등)로 받아보는 것.
  - 아직 구현 시점은 아님 — 사용자가 메신저 기능 추가를 별도로 요청하면 그때 진행.

## 2026-09-18 — 유사도 판정(로드맵 4~5단계) 논의 정리 — 월요일 이어서 진행

목표: 신규 공고를 회사 과거 실적·제안서와 비교해 "우리가 할 만한 일인가" 유사도 판정하는
기능 추가. 오늘은 설계·데이터 조사만 하고 실제 파이프라인 연동은 아직 안 함.

### 결론: 별도 레포 `jiil-past-contracts`에 평가 엔진 구현함

[naipchal.kr/methodology](https://naipchal.kr/methodology)(나라장터 공고 매칭 서비스 "나입찰"의
공개 방법론)를 벤치마킹해서 지일 전용으로 이식함:

- **참가 가능성 / 수행 적합성을 분리 판정** — `evaluation/gate.py`(마감·지역·필수면허·
  실적제한, 게이트 — 미달이면 점수 보정 없이 바로 "참가 불가") / `evaluation/suitability.py`
  (과업-주력분야·동일분야실적·금액-수행규모, 게이트 통과 후에만 계산)
- **모든 판정은 충족/미달/미확인 3상태로만 표현** — 정보 없음을 0점·탈락으로 임의 변환 안 함
- **공동수급 허용 공고는 규모초과를 "미달" 대신 "공동수급으로 보완 가능"한 미확인으로 완화**
- **등급 3단계**: 우선 검토 / 조건 확인 / 참가 불가 (`evaluation/assess.py`)
- 회사 프로필(`config/company_profile.json`)은 이 레포의 keywords.json/held_qualifications.json
  값으로 시드해둠. `region`은 null — 실제로는 강원도 정선(수행능력평가서에서 확인)이라
  다음에 채워야 함.
- 테스트 36개, 나입찰 방법론 문구를 그대로 인용해 원칙을 검증함.
- **아직 실제 공고·과거실적 데이터와는 연결 전** — 독립 모듈로만 존재.

### 과거 자료(제안서·수행능력평가서) 조사 결과

- **제안서 PDF는 텍스트 레이어가 살아있어 pypdf로 바로 추출 가능** — 실측 2/2 성공
  (횡성목재문화체험장, 4.16생명안전공원). 목차·세부연출계획까지 깨끗하게 나옴.
- **수행능력평가서는 텍스트/스캔 혼재** — 파일당 40~60% 스캔. 텍스트 구간도 문서 종류가
  다양해서(사업장 가입자 명부=인력증빙, 일반현황 등) 단순 패턴이 아님.
- **첨부 서식이 파일마다 다름**:
  - "사업수행실적집계표"(서식 제13호-1) — 사업명·발주기관·납품액(천원 단위!)이 표로
    정리된 원자료. 있으면 제일 좋음.
  - "사업수행능력 자기평가표"(서식 제17호) — 회사가 그 공고 배점기준에 맞춰 스스로
    매긴 점수표. 원자료는 없지만 **발주기관이 실제로 쓰는 배점 구간**(예: 최근 5년
    유사실적 누적금액 9억↑=만점, 1억 미만 단일실적 없음=최저점)이 담겨 있어, 지금
    `suitability.py`의 "1.5배 규모면 충족"이라는 **임의 기준을 실증 데이터로 보정**하는
    데 쓸 수 있음. 모든 폴더에 둘 다 있는 게 아니라 공고마다 요구 서식이 다름 —
    추출 스크립트는 여러 서식 후보를 다 찾아보는 식으로 짜야 함.
- **HWP 파일(제안요청서/과업지시서) 파싱 검증됨** — `nego/attachments.py`의
  `_extract_hwp_text`(olefile 기반)로 실제 업로드 파일 2건에서 성공적으로 텍스트 추출.
  "양남 물빛사랑교 상징조형물" 제안요청서+과업지시서에서 구체적 과업
  키워드("상징조형물", "버스킹 공연장", "경관조명"—설계만 대상, "구조기술사 검토" 등)
  까지 뽑아냄. 세부품명번호(6012100201 조형물)까지 확인됨.
  - **주의**: 같은 키워드("내용적 범위" 등)가 목차와 본문에 중복 등장해서, 처음 나오는
    위치만 보면 목차를 잘못 짚을 수 있음. 자동화할 때 "목차 구간을 건너뛰고 진짜 챕터
    본문을 찾는" 로직이 필요 — `qualification_text.py`가 이미 비슷한 문제(참가자격 절
    찾기)를 다루고 있어서 그 패턴 참고 가능.
- **첨부파일이 하나뿐인 공고**(제안요청서가 별도로 없어 보이는 경우) — 공고문 파일 하나에
  전체 내용(참가자격+과업내용)이 다 들어있는 경우가 많음. "없다"고 단정하지 말고 그 파일
  내부 목차부터 확인할 것. 그 외 가능성: 발주기관 자체 홈페이지 게시판, 정정·변경공고로
  나중에 추가.

### 계약정보서비스 API 실측 (`jiil-past-contracts/verify.py`)

- 살아있는 오퍼레이션: `getCntrctInfoListThng`/`Servc`/`Cnstwk` (계약정보서비스,
  `https://apis.data.go.kr/1230000/ao/CntrctInfoService`)
- `baseDtls` 필드에 "협상에 의한 계약" 같은 값 확인됨 — 낙찰방법 신호로 쓸 수 있을 듯
- **회사(사업자등록번호) 필터 파라미터를 아직 못 찾음** — 후보 8개
  (`bidwinnrBizno`, `cntrctCorpBizno`, `corpBizno`, `bizno`, `prtcptBizno`,
  `cntrctCorpNm`, `corpNm`, `bidwinnrNm`) 전부 totalCount 불변으로 실패.
  다음 시도: data.go.kr 상세페이지의 정확한 요청변수 목록 확인, 또는 g2b.go.kr 로그인 후
  "낙찰내역 조회" 메뉴 직접 확인(이게 더 빠를 수 있음).
- 계약과정통합공개서비스(`CntrctProcssIntgOpenService`)는 날짜범위 목록이 아니라
  공고번호 하나로 조회하는 서비스로 보임 — 아직 이 방식으로 테스트 안 함.

### 월요일에 이어갈 것 (우선순위 순)

1. **회사 필터 방법 확정** — API 파라미터든 g2b.go.kr 로그인이든, 이게 뚫려야
   `PastProject` 풀을 채울 수 있음(제일 큰 병목).
2. **과거실적 데이터 실제로 채우기** — 위 API/로그인 + 수행능력평가서 "사업수행실적집계표"
   조합. `config/past_projects.json`(negotiate 레포)에 넣거나 jiil-past-contracts 쪽
   자료구조로 옮기기.
3. **섹션 추출기** — 목차/본문 구분 로직 포함해서 제안요청서·과업지시서에서
   사업개요·과업내용·수행범위를 안정적으로 뽑는 파서.
4. **실적제한 금액 파싱** — 지금 `evaluation.Notice.min_track_record_amount`를 채울
   방법이 없음. 참가자격 절에 "최근 N년 실적 O억 이상" 같은 문구를 새로 파싱해야 함.
5. ~~**내용 유사도 — AI 보조 판단 여부 결정**~~ — 2026-09-21 구현 완료. 아래 섹션 참고.
6. **`suitability.py`의 "1.5배 규모" 임계값 보정** — 서식 제17호(자기평가표) 데이터를
   여러 건 모아서 임의값 대신 실증 구간으로 교체.
