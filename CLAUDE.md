# 프로젝트 메모

## 2026-09-16 수정분 실측 검증 — 부분 확인됨 (2026-09-18, 5일치 수집으로)

30일치 정식 검증은 아직 안 했고, 9/18에 5일치(9/13~9/18) 수집으로 일부만 확인됨.

1. **단양군 미디어아트 공고(R26BK01731335) "자격 미달(조명용제어장치(3912110702))"** — ✅ 확인됨.
   9/18 Run #64에서 정확히 이 문구로 표시됨.
2. **오탐 확인** — 5일치 표본이 작아(참가자격 절 발견 7건 중 판정 반영 3건) 아직 전수 대조 못 함.
   30일치로 다시 돌릴 때 재확인할 것.
3. **경상남도관광재단 K-거상 공고(R26BK01707504) "마감/일정"** — ✅ 확인됨.
   9/18 Run #64에서 "D-10 (첨부파일 제출기한)"으로 정확히 채워짐.

## 결정된 사항 (아직 구현 전, 요청 시 진행)

- **메신저 알림 플랫폼: 텔레그램(Telegram) 봇**으로 결정. 카카오톡은 비즈니스 채널+알림톡 등록·템플릿 심사·건당 비용이 필요해 보류하고, 완전 무료에 승인 절차 없이 바로 되는 텔레그램 봇 API로 진행하기로 함.
  - 목적: 매주 수집 결과를 "수집 완료" 알림이 아니라 **보고서 형태**(후보 목록·발주기관·금액·마감일 등)로 받아보는 것.
  - 아직 구현 시점은 아님 — 사용자가 메신저 기능 추가를 별도로 요청하면 그때 진행.

## 2026-09-18 — 유사도 판정(로드맵 4~5단계) 논의 정리 — 월요일 이어서 진행

목표: 신규 공고를 회사 과거 실적·제안서와 비교해 "우리가 할 만한 일인가" 유사도 판정하는
기능 추가. 오늘은 설계·데이터 조사만 하고 실제 파이프라인 연동은 아직 안 함.

### 결론: 별도 레포 `jiil-past-contracts`에 평가 엔진 구현함

[naipchal.kr/methodology](https://naipchal.kr/methodology)(나라장터 공고 매칭 서비스 "나입찰"의
공개 방법론)를 벤치마킹해서 지일 전용으로 이식함:

- **참가 가능성 / 수행 적합성을 분리 판정** — `evaluation/gate.py`(마감·지역·필수면허·
  실적제한, 게이트 — 미달이면 점수 보정 없이 바로 "참가 불가") / `evaluation/suitability.py`
  (과업-주력분야·동일분야실적·금액-수행규모, 게이트 통과 후에만 계산)
- **모든 판정은 충족/미달/미확인 3상태로만 표현** — 정보 없음을 0점·탈락으로 임의 변환 안 함
- **공동수급 허용 공고는 규모초과를 "미달" 대신 "공동수급으로 보완 가능"한 미확인으로 완화**
- **등급 3단계**: 우선 검토 / 조건 확인 / 참가 불가 (`evaluation/assess.py`)
- 회사 프로필(`config/company_profile.json`)은 이 레포의 keywords.json/held_qualifications.json
  값으로 시드해둠. `region`은 null — 실제로는 강원도 정선(수행능력평가서에서 확인)이라
  다음에 채워야 함.
- 테스트 36개, 나입찰 방법론 문구를 그대로 인용해 원칙을 검증함.
- **아직 실제 공고·과거실적 데이터와는 연결 전** — 독립 모듈로만 존재.

### 과거 자료(제안서·수행능력평가서) 조사 결과

- **제안서 PDF는 텍스트 레이어가 살아있어 pypdf로 바로 추출 가능** — 실측 2/2 성공
  (횡성목재문화체험장, 4.16생명안전공원). 목차·세부연출계획까지 깨끗하게 나옴.
- **수행능력평가서는 텍스트/스캔 혼재** — 파일당 40~60% 스캔. 텍스트 구간도 문서 종류가
  다양해서(사업장 가입자 명부=인력증빙, 일반현황 등) 단순 패턴이 아님.
- **첨부 서식이 파일마다 다름**:
  - "사업수행실적집계표"(서식 제13호-1) — 사업명·발주기관·납품액(천원 단위!)이 표로
    정리된 원자료. 있으면 제일 좋음.
  - "사업수행능력 자기평가표"(서식 제17호) — 회사가 그 공고 배점기준에 맞춰 스스로
    매긴 점수표. 원자료는 없지만 **발주기관이 실제로 쓰는 배점 구간**(예: 최근 5년
    유사실적 누적금액 9억↑=만점, 1억 미만 단일실적 없음=최저점)이 담겨 있어, 지금
    `suitability.py`의 "1.5배 규모면 충족"이라는 **임의 기준을 실증 데이터로 보정**하는
    데 쓸 수 있음. 모든 폴더에 둘 다 있는 게 아니라 공고마다 요구 서식이 다름 —
    추출 스크립트는 여러 서식 후보를 다 찾아보는 식으로 짜야 함.
- **HWP 파일(제안요청서/과업지시서) 파싱 검증됨** — `nego/attachments.py`의
  `_extract_hwp_text`(olefile 기반)로 실제 업로드 파일 2건에서 성공적으로 텍스트 추출.
  "양남 물빛사랑교 상징조형물" 제안요청서+과업지시서에서 구체적 과업
  키워드("상징조형물", "버스킹 공연장", "경관조명"—설계만 대상, "구조기술사 검토" 등)
  까지 뽑아냄. 세부품명번호(6012100201 조형물)까지 확인됨.
  - **주의**: 같은 키워드("내용적 범위" 등)가 목차와 본문에 중복 등장해서, 처음 나오는
    위치만 보면 목차를 잘못 짚을 수 있음. 자동화할 때 "목차 구간을 건너뛰고 진짜 챕터
    본문을 찾는" 로직이 필요 — `qualification_text.py`가 이미 비슷한 문제(참가자격 절
    찾기)를 다루고 있어서 그 패턴 참고 가능.
- **첨부파일이 하나뿐인 공고**(제안요청서가 별도로 없어 보이는 경우) — 공고문 파일 하나에
  전체 내용(참가자격+과업내용)이 다 들어있는 경우가 많음. "없다"고 단정하지 말고 그 파일
  내부 목차부터 확인할 것. 그 외 가능성: 발주기관 자체 홈페이지 게시판, 정정·변경공고로
  나중에 추가.

### 계약정보서비스 API 실측 (`jiil-past-contracts/verify.py`)

- 살아있는 오퍼레이션: `getCntrctInfoListThng`/`Servc`/`Cnstwk` (계약정보서비스,
  `https://apis.data.go.kr/1230000/ao/CntrctInfoService`)
- `baseDtls` 필드에 "협상에 의한 계약" 같은 값 확인됨 — 낙찰방법 신호로 쓸 수 있을 듯
- **회사(사업자등록번호) 필터 파라미터를 아직 못 찾음** — 후보 8개
  (`bidwinnrBizno`, `cntrctCorpBizno`, `corpBizno`, `bizno`, `prtcptBizno`,
  `cntrctCorpNm`, `corpNm`, `bidwinnrNm`) 전부 totalCount 불변으로 실패.
  다음 시도: data.go.kr 상세페이지의 정확한 요청변수 목록 확인, 또는 g2b.go.kr 로그인 후
  "낙찰내역 조회" 메뉴 직접 확인(이게 더 빠를 수 있음).
- 계약과정통합공개서비스(`CntrctProcssIntgOpenService`)는 날짜범위 목록이 아니라
  공고번호 하나로 조회하는 서비스로 보임 — 아직 이 방식으로 테스트 안 함.

### 월요일에 이어갈 것 (우선순위 순) — ~~2026-09-18 작성~~ 아래 2026-09-23 항목으로 대체/갱신됨

1. ~~회사 필터 방법 확정~~ — 보류. 대신 과거실적 원문(제안요청서·과업지시서)에서
   직접 업역/금액을 파싱하는 쪽으로 방향을 바꿨음(아래 참고).
2. **과거실적 데이터 실제로 채우기** — 1차 완료(아래 2026-09-23 항목), 2차(업역·규모
   `config/past_projects.json` 반영)는 다음 세션 1순위.
3. **섹션 추출기** — ✅ 완료. `nego/section_text.py`(2026-09-23 도입, 아래 참고).
4. **실적제한 금액 파싱** — 아직 미착수.
5. **내용 유사도 — AI 보조 판단** — 아직 미착수. 지금은 IDF 가중 오버랩(아래 참고)까지만.
6. **`suitability.py`의 "1.5배 규모" 임계값 보정** — 아직 미착수(jiil-past-contracts
   docs/scoring_bands/달성군보건소.md에 실측 배점표 1건 확보해둠 — 더 모아야 함).

## 2026-09-23 — 싱크로율(과거 실적 유사도) 1차 구현 + 과거실적 78건 실데이터화

**다음 세션 1순위 할 일** (이것부터):
`jiil-past-contracts` 레포 `docs/summaries/2017~2021.md`에 78건 전부 **업역**(참가자격
업종명/세부품명번호)·**규모**(사업금액) 필드가 이미 채워져 있음(오늘 재수집 완료). 이걸
`config/past_projects.json`의 `industryNames`/`productCodes`/`amount` 필드에 반영하고,
`nego/cli.py`의 `score_similarity(..., weights=TEXT_ONLY_WEIGHTS)`를
`weights=DEFAULT_WEIGHTS`(균등 3분할)로 되돌릴 것 — `TEXT_ONLY_WEIGHTS`는 저 두 필드가
비어있던 임시 조치였음(`nego/similarity.py`의 `TEXT_ONLY_WEIGHTS` 주석 참고). 삼탄역
관광테마공원 건은 원문에 "사업비 300,000,000천원"으로 오기돼 있음 — 3억원(300,000,000)으로
정규화해서 넣을 것(그대로 넣으면 3천억원이 됨).

**과거실적 데이터 (jiil-past-contracts)**:
- `docs/raw/`(원본 파일)·`docs/extracted/`(pypdf/OCR 추출 텍스트)에서 81개 프로젝트 폴더 중
  78건을 `docs/summaries/2017~2021.md`로 정리(면적/과업내용 키워드/전시내용/업역/규모 5필드).
  2017년치는 이 세션이 직접, 2018~2021년치(70건)는 서브에이전트 4개(연도별)에 위임 후
  검증. 3건 제외: **달성군보건소**·**괴산내륙어촌토종어류**(사업수행능력평가서 뿐이고
  참가자격/사업금액 자체가 원문에 없음 — OCR까지 돌려서 재확인함), **태백 오로라파크**
  (raw 폴더 파일이 실제로는 "태백 슬로우레스토랑" 2016년 기본실시설계 문서였음 — 데이터
  수집 단계 오분류로 추정, 사용자가 원본 파일 삭제함).
- **주의**: 서브에이전트 결과 78건 중 73건은 이 세션이 원문과 직접 재대조하지 않았음(포맷·
  블로커 개수만 확인). 실제로 1건(남부창의인재교육지원센터)에서 과업내용 키워드 누락을
  사용자가 발견해서 고침 — 비슷한 누락이 더 있을 수 있음.
- `docs/scoring_bands/달성군보건소.md`: 사업수행능력 자기평가표(서식 제10호) 배점구간
  실측 1건 — `suitability.py` "1.5배 규모" 보정용 참고자료로 남겨둠(아직 반영 안 함).

**싱크로율 구현 (negotiate 레포)**:
- `nego/similarity.py`: 자카드 → **IDF 가중 오버랩 계수**로 교체(`_content_overlap`,
  `_document_frequencies`). 자카드는 과거사업 요약이 길수록 분모가 커져 매칭이
  희석되는 문제가 실측으로 확인됨(예: "어린이과학관 야외 전시체험물" 검색이 무관한
  "단양 IC"에 매칭됨) — 오버랩 계수(분모=공고 쪽 토큰만) + IDF(흔한 단어 감점)로
  해결. 실측: 같은 질의가 "충주 어린이과학관 야외전시콘텐츠" 45.4%로 정확히 매칭됨.
  `score()`에 `extra_text` 파라미터 추가(기본값 "" — 기존 호출 전부 하위호환).
- `nego/section_text.py` **(신규, 이 레포에 처음 커밋됨)**: 원문을 최상위 절 단위로
  나누는 모듈. 같은 저장소 `claude/modest-faraday-gw058m` 브랜치에 이미 107건 실측
  (98% 인식률)으로 검증된 버전이 있어 그대로 가져옴 — 목차/본문 혼동, 로마숫자 목차
  오탐, 원문자(PUA 글리프) 절 등을 다룸. **알려진 한계**: 절 번호가 문서 중간에
  리셋되는 문서(실측: 남부창의인재교육지원센터 제안요청서 — 본문 Ⅰ~Ⅳ 다음에
  사업수행능력평가서 부분이 또 Ⅰ부터 시작)는 못 찾고 fail-open(빈 결과)됨 — 흔치
  않은 케이스라 지금은 안 고침.
- `nego/attachments.py`: `save_attachment_texts()`가 첨부파일 원문에서 "과업내용/범위"·
  "전시내용/구성" 절도 찾아 `candidate.content_task_text`/`content_exhibit_text`에
  남김(참가자격 절 찾기와 같은 자리, `--fetch-attachment-text`일 때만). 면적(㎡) 언급도
  `content_area_note`로 남기지만 점수엔 안 씀(신규 공고 쪽 면적 파싱이 검증된 적 없어
  숫자 하나가 우연히 맞으면 왜곡될 위험 — 과거실적 쪽 `area_note`와 같은 이유).
- `nego/cli.py`: `candidate.content_task_text + content_exhibit_text`를 `extra_text`로
  넘겨 제목뿐 아니라 첨부파일 본문까지 매칭에 씀. `--fetch-attachment-text` 없이 돌리면
  이 필드가 비어있어 제목만 쓰는 기존 동작 그대로 — 하위호환됨.
- `nego/report.py`: HTML 리포트를 다크 테마로 재디자인(기존 HTML 구조/클래스명은
  `tests/test_report.py` 호환을 위해 그대로 유지, CSS만 교체) + 강력추천/참고용 필터
  칩(클라이언트 사이드 동작) + 싱크로율 배지(호버 시 매칭 사업명·면적·업역/규모/내용
  축별 점수 팝업).
- 테스트 212개 전부 통과(section_text.py 테스트 18개 포함). 하지만 **실제 API로 신규
  공고를 수집해서 `--fetch-attachment-text`까지 돌려본 적은 없음** — 픽스처/단위테스트
  로만 확인됨. 다음 세션에서 실제 공고로 한 번 실측 검증할 것.
