# 프로젝트 메모

## 결정된 사항 (아직 구현 전, 요청 시 진행)

- **메신저 알림 플랫폼: 텔레그램(Telegram) 봇**으로 결정. 카카오톡은 비즈니스 채널+알림톡 등록·템플릿 심사·건당 비용이 필요해 보류하고, 완전 무료에 승인 절차 없이 바로 되는 텔레그램 봇 API로 진행하기로 함.
  - 목적: 매주 수집 결과를 "수집 완료" 알림이 아니라 **보고서 형태**(후보 목록·발주기관·금액·마감일 등)로 받아보는 것.
  - 아직 구현 시점은 아님 — 사용자가 메신저 기능 추가를 별도로 요청하면 그때 진행.

- **나라장터 사전규격정보서비스(사전규격) API 추가 수집.** (2026-09-15 결정)
  - 조건부: 현재 진행 중인 30일 테스트(claude/sweet-hypatia-8pwz6c, run 34935236571)가 성공적으로 끝나면 착수.
  - 아직 구현 시점은 아님 — 30일 테스트 결과 확인 후 진행.

- **PoC4(협상에의한계약 자격요건 스캔 규칙 검증) 방향 결정.** (2026-09-15 결정, 레포 밖 별도 문서 기반 계획)
  - 등록업종 스캔 규칙은 (a) 면허제한정보 API가 비어 있을 때의 **보완 로직**으로 간다 — 즉 `nego/qualification_text.py`가 현재 하는 역할(첨부파일 텍스트에서 "N. 입찰참가자격" 절 추출)을 실적/등록업종/신용등급으로 **분류하는 기능으로 확장**하는 방향. API와 별개로 모든 공고를 텍스트로 이중 판정하는 방향은 아님.
  - 실적·신용평가등급 스캔 규칙(신규 영역, 코드베이스에 아직 대응 로직 없음)과 "검증 완료" 판정 기준 정의는 추후로 미룸.
  - 아직 구현 시점은 아님 — PoC4 자체 체크리스트(표본 수집·키워드 규칙 초안·표본 대조 검증)가 레포 밖에서 먼저 끝나야 코드 작업을 시작할 수 있음.

## 보류된 검토 사항 (건들지 않기로 함)

- **`config/keywords.json`의 `excludeKeywords` 과잉매칭 발견, 수정은 보류.** (2026-09-15)
  - 30일 수집 결과(16건)가 기존 주간 이메일 시스템(narajangter-bid-monitor)의 "주간 12건"보다 훨씬 적어 보여서 원인 분석함.
  - **결론: 주된 원인은 버그가 아니라 스코프 차이.** 기존 시스템의 "12건"은 계약방식과 무관하게 전체 나라장터 공고를 스캔한 결과였고(예시 리포트에 "지명경쟁" 공고도 포함돼 있었음), 실제로 "협상에 의한 계약"이었던 건 12건 중 2건뿐이었다 — 그 2건은 이 시스템도 후보로 정확히 잡아냈다. 즉 협상계약이라는 좁은 스코프 자체가 원래 이런 비율 차이를 만든다.
  - **부수적으로 발견한 진짜 과잉매칭**: exclude 키워드 `"특별전"`/`"순회전"`이 의도(유물 대여 특별전의 포장/운송/철수/해포 용역 거르기)보다 넓게 걸려서, "전시물 제작‧설치" 같은 JIIL 핵심 업무 공고까지 최소 3건 실측으로 잘못 제외시키고 있음을 확인함. `포장`/`운송`/`철수`/`해포`가 이미 별도 exclude 키워드로 있어서 "특별전"/"순회전"을 빼도 원래 취지는 그대로 지켜짐 — 그런데도 사용자가 **기존 로직은 건드리지 말고 일단 수집 시스템 구현을 마친 뒤 상사와 상의해서 수정하기로 결정**함. `연출`/`고도화`/`운영 용역` 등 다른 넓은 키워드도 같은 방식으로 검토는 했으나 마찬가지로 보류.
  - **다음에 이 얘기가 다시 나오면**: `excludeKeywords`에서 `특별전`/`순회전` 제거를 1순위 후보로 검토할 것 (근거는 위 표, 대화 기록 참고). 코드/설정 변경은 아직 하지 않았음.

- **`minBudgetAmount` 비교 시 부가세 포함 여부 문제 발견, 수정은 보류.** (2026-09-15)
  - 실측 사례: 우정사업조달센터 "기념우표 기획전시관 조성 용역"(R26BK01700012) — `presmptPrce`(추정가격, VAT 제외) 96,400,000원이라 1억원 미만으로 제외됐지만, `asignBdgtAmt`(배정예산, VAT 포함 실제 총액)은 106,040,000원으로 이미 1억을 넘음.
  - `nego/models.py`의 `Notice.budget`은 "추정가격 우선, 없으면 배정예산"으로 계산하는데(기존 시스템 로직 그대로), 이 케이스처럼 추정가격(VAT 제외)만 놓고 보면 컷오프 바로 아래인데 VAT 포함 총액은 컷오프를 넘는 공고를 놓칠 수 있음.
  - 예산 필터도 `excludeKeywords`와 같은 이유로 "기존 시스템과 동일 규칙 유지" 정책 하에 있어 임의로 바꾸지 않음. 다음에 예산 기준 얘기가 나오면 "VAT 포함 총액(배정예산) 기준으로 비교하는 게 맞는지" 상사와 확인 필요.

## 방침 변경

- **지역제한 필터링은 하지 않는다. 전국 대상으로 수집한다.** (2026-09-15 결정)
  - 참가가능지역 API(`fetch_region_limits`)는 계속 조회해서 정보로만 붙이되(기존 동작 유지), 이걸로 공고를 걸러내는 필터는 만들지 않는다.
  - 기존에 검토 중이던 "지역제한을 실제 필터로 추가 (첨부파일 파싱 필요 추정)" 항목은 폐기.

- **첨부파일 텍스트의 담당자 연락처(전화번호/이메일) 자동 마스킹 기능을 제거한다.** (2026-09-15 결정)
  - 공개공고에 실린 발주기관 문의처는 가려야 할 개인정보로 보지 않기로 함(공개공고이므로 상관없다는 판단).
  - `nego/attachments.py`의 `redact_personal_contacts()`와 관련 정규식·테스트를 제거함. `extract_text()`는 이제 원문 그대로 반환.
  - 단, 이건 첨부파일 본문 텍스트에 한정된 변경이다. API 원문(raw) 저장 전 `nego/fields.py`의 `PERSONAL_FIELDS`(담당자 이름·전화·이메일 필드 제거)는 **별개 기능이라 그대로 유지**한다 — 이번에 건드리지 않았음.
