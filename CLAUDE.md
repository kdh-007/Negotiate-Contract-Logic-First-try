# 프로젝트 메모

## 다음에 할 일 — 2026-09-16 수정분 실측 검증 (내일 오전)

30일치 수집(`Actions → 협상공고 일일 수집 → Run workflow`, days=30)을 돌려서 아래를 확인할 것.
**아직 실제 수집으로 검증되지 않은 변경들이다** (단위 테스트와 수동 검증만 끝난 상태).

1. **단양군 미디어아트 공고(R26BK01731335)가 "자격 미달(조명용제어장치(3912110702))"로 바뀌었는지** —
   직전 실행(Run #42)까지는 "자격 충족"으로 잘못 표시됐다. 원인은 API 면허제한정보에
   세부품명번호 필드가 없어서 품목 요건이 통째로 빠지는데도, 첨부파일 판정을
   "API가 아무것도 안 줬을 때만" 돌렸기 때문(PR #26에서 `qualify.merge_results`로 병합하도록 수정).
2. **오탐 확인** — 첨부파일 파싱이 이제 참가자격 절을 찾은 **전체** 공고에 적용된다
   (Run #42 기준 3건 → 32건). 파싱이 코드를 잘못 뽑으면 멀쩡한 공고가 "자격 미달"로 뜰 수 있다.
   미달로 뜬 건은 `output/attachment_text/<공고번호>_..._참가자격.txt` 원문과 대조해 볼 것.
3. **경상남도관광재단 K-거상 공고(R26BK01707504)의 "마감/일정"** — API 마감 3필드가 전부 비어
   "일정 미상"이던 공고에 첨부파일에서 뽑은 제출기한이 채워지는지(라벨: "첨부파일 제출기한").

## 결정된 사항 (아직 구현 전, 요청 시 진행)

- **메신저 알림 플랫폼: 텔레그램(Telegram) 봇**으로 결정. 카카오톡은 비즈니스 채널+알림톡 등록·템플릿 심사·건당 비용이 필요해 보류하고, 완전 무료에 승인 절차 없이 바로 되는 텔레그램 봇 API로 진행하기로 함.
  - 목적: 매주 수집 결과를 "수집 완료" 알림이 아니라 **보고서 형태**(후보 목록·발주기관·금액·마감일 등)로 받아보는 것.
  - 아직 구현 시점은 아님 — 사용자가 메신저 기능 추가를 별도로 요청하면 그때 진행.
