# 프로젝트 메모

## 결정된 사항 (아직 구현 전, 요청 시 진행)

- **메신저 알림 플랫폼: 텔레그램(Telegram) 봇**으로 결정. 카카오톡은 비즈니스 채널+알림톡 등록·템플릿 심사·건당 비용이 필요해 보류하고, 완전 무료에 승인 절차 없이 바로 되는 텔레그램 봇 API로 진행하기로 함.
  - 목적: 매주 수집 결과를 "수집 완료" 알림이 아니라 **보고서 형태**(후보 목록·발주기관·금액·마감일 등)로 받아보는 것.
  - 아직 구현 시점은 아님 — 사용자가 메신저 기능 추가를 별도로 요청하면 그때 진행.

## 방침 변경

- **지역제한 필터링은 하지 않는다. 전국 대상으로 수집한다.** (2026-09-15 결정)
  - 참가가능지역 API(`fetch_region_limits`)는 계속 조회해서 정보로만 붙이되(기존 동작 유지), 이걸로 공고를 걸러내는 필터는 만들지 않는다.
  - 기존에 검토 중이던 "지역제한을 실제 필터로 추가 (첨부파일 파싱 필요 추정)" 항목은 폐기.

- **첨부파일 텍스트의 담당자 연락처(전화번호/이메일) 자동 마스킹 기능을 제거한다.** (2026-09-15 결정)
  - 공개공고에 실린 발주기관 문의처는 가려야 할 개인정보로 보지 않기로 함(공개공고이므로 상관없다는 판단).
  - `nego/attachments.py`의 `redact_personal_contacts()`와 관련 정규식·테스트를 제거함. `extract_text()`는 이제 원문 그대로 반환.
  - 단, 이건 첨부파일 본문 텍스트에 한정된 변경이다. API 원문(raw) 저장 전 `nego/fields.py`의 `PERSONAL_FIELDS`(담당자 이름·전화·이메일 필드 제거)는 **별개 기능이라 그대로 유지**한다 — 이번에 건드리지 않았음.
